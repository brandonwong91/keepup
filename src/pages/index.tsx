import { SignIn, SignOutButton, useUser } from "@clerk/nextjs";
import { type NextPage } from "next";
import Head from "next/head";
// import Link from "next/link";

// import { api } from "~/utils/api";

const Home: NextPage = () => {
  // const hello = api.example.hello.useQuery({ text: "from tRPC" });
  const user = useUser();
  console.log("user", user);
  return (
    <>
      <Head>
        <title>Create T3 App</title>
        <meta name="description" content="Generated by create-t3-app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <main className="flex min-h-screen flex-col items-center justify-center bg-gradient-to-b from-[#2e026d] to-[#15162c]">
        {!user.isSignedIn && <SignIn />}
        {!!user.isSignedIn && user.user.firstName && (
          <div className="flex h-20 flex-col gap-x-1 rounded-lg bg-gray-100 p-2 text-slate-400">
            {`Hi there, ${user.user.firstName}`}
            <div className="self-center rounded-lg border-2 p-2 hover:border-2 hover:border-red-300 hover:text-red-300">
              <SignOutButton />
            </div>
          </div>
        )}
      </main>
    </>
  );
};

export default Home;
